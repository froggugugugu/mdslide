# セキュリティ

## 対応する版

直すのは最新のリリースだけです。古い版で気づいた場合は、最新版でも起きるかを確かめてから知らせてください。

## 脆弱性の知らせ方

公開の Issue には書かず、GitHub の非公開の報告（[Report a vulnerability](https://github.com/froggugugugu/mdslide/security/advisories/new)）を使ってください。再現の手順、起きること、試した版を書いてもらえると助かります。

個人で保守しているため、返信や修正の時期は約束できません。確かめて直す場合は、リリースノートと Security Advisory でお知らせします。

## 配布物の確かめ方

- リリースは GitHub の Immutable releases で公開しています。公開した後は、添付ファイルもタグも変えられません（ADR-0027）。
- インストールコマンド（`install.sh`）は、ダウンロードした zip の sha256 を GitHub がリリースに記録した値と照らし合わせ、署名を確かめてから入れます。
- 手で確かめる場合は、`gh release verify v<版>` と `gh release verify-asset v<版> <ファイル>` を使えます。

## 設計上の前提

- mdslide が読み書きするのは、開いた資料フォルダ、マスターの保管フォルダ、設定ファイルだけです。deck.md の画像も、資料フォルダの中の相対パスだけを表示し、書き出します（ADR-0026）。
- コンソールは本物のターミナルです。下書きのボタンは、コンソールでツールが動いているときだけ定型の指示を送ります。
- 配布アプリはアドホック署名で、Apple の公証は受けていません（ADR-0020）。
